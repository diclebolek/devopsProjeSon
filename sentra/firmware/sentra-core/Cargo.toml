[package]
name = "sentra-core"
version = "0.1.0"
edition = "2021"
description = "Host-testable DSP, sensor decoders, and telemetry contract for SENTRA"
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
